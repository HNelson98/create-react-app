const initialstate ={
    data: [],
    isFetching: false,
    error: ""


}

export const reducer =( state = initialstate, action) =>{
    switch(action.type){
        default:
            return state
    }
}